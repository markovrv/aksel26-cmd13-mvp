# ESP32-CAM Tank Robot

Робот-машинка на базе ESP32-CAM (AI-Thinker) с дистанционным управлением через WebSocket и видеотрансляцией MJPEG по Wi-Fi. 
Проект использует два отдельных канала связи: **WebSocket (порт 82)** для низколатентного управления движением и **HTTP MJPEG (порт 81)** для стабильного видеопотока с камеры. 
Веб-интерфейс загружается с SD-карты и работает как PWA — открывается в браузере на телефоне или ПК без установки приложений.

**Возможности:**
- Управление с телефона через виртуальный джойстик (nipplejs) — одним пальцем
- Управление с клавиатуры (WASD) — для ПК
- Плавный разгон и торможение на ESP32
- Танковая схема управления (раздельные левый/правый моторы)
- Авто-реконнект WebSocket при обрыве связи
- Настройка качества видео, разрешения, FPS, яркости и контраста
- LED-вспышка для камеры

## Архитектура

```
ESP32-CAM (AI-Thinker)
 ├─ HTTP сервер (порт 80)  — статика (HTML/CSS/JS из SD→RAM) + /config
 ├─ MJPEG stream (порт 81) — только видеопоток камеры (HTTP, multipart/x-mixed-replace)
 └─ WebSocket сервер (порт 82) — управление движением (JSON)
```

### Порты
| Порт | Протокол | Назначение |
|------|----------|------------|
| 80   | HTTP     | Статические файлы (index.html, style.css, app.js, nipplejs.min.js) + endpoint /config |
| 81   | HTTP     | MJPEG видеопоток с камеры (multipart/x-mixed-replace) |
| 82   | WebSocket| Приём команд управления: `{"x":-100..100,"y":-100..100,"t":timestamp}` |

### Управление движением
- **Джойстик** (мобильные устройства): nipplejs → WebSocket → ESP32 → L298N
- **Клавиатура WASD** (ПК): keydown/keyup → плавный разгон → WebSocket → ESP32 → L298N
- **Танковая схема**: left = y + x, right = y - x, нормализация при >100%
- **Чистый поворот**: при |y| < 10% и |x| ≥ 10% — разворот на месте

### Настройки (HTTP /config)
- `accel` — интервал шага разгона (мс)
- `quality` — качество JPEG (4-63)
- `framesize` — разрешение камеры (0-10)
- `brightness` — яркость (-2..2)
- `contrast` — контраст (-2..2)
- `fps` — частота кадров видео (1-30)
- `flash` — LED вспышки (0/1)

### Низкая задержка
- `WiFi.setSleep(false)` — отключение энергосбережения WiFi
- `setNoDelay(true)` — отключение Nagle (TCP_NODELAY) на WebSocket сокетах
- Throttle команд на клиенте (50 мс) — отправка только последнего состояния
- `sendWsCommandImmediate()` — немедленная отправка (для остановки)

### Надёжность
- Авто-реконнект WebSocket (2 сек)
- Остановка моторов при потере соединения WS
- Heartbeat WebSocket (5s ping, 6s timeout)
- Остановка моторов при потере фокуса окна (blur)
- Плавный разгон/торможение на ESP32

## Подключение
- WiFi: SSID = `11935`, пароль = `9091433506`
- Плата: ESP32-CAM AI-Thinker
- Мотор-драйвер: L298N (IN1=GPIO2, IN2=GPIO14, IN3=GPIO15, IN4=GPIO13)
- Вспышка: GPIO4
- Файлы на SD-карте в папке `/www`

## Зависимости Arduino IDE
- `esp32` by Espressif Systems (платформа)
- `WebSockets` by Markus Sattler (для WebSocketsServer)

## Статические файлы на SD
```
/www/
 ├── index.html
 ├── style.css
 ├── app.js
 └── nipplejs.min.js
```